language: c
sudo: false

addons:
  apt:
    sources:
    - avsm
    packages:
    - aspcud
    - opam

script: bash -ex .travis.sh
env:
  matrix:
  - OCAML_VERSION=4.04.2
  - OCAML_VERSION=4.04.2+fp+flambda
  - OCAML_VERSION=4.05.0
  - OCAML_VERSION=4.05.0+flambda
  - OCAML_VERSION=4.06.0
  - OCAML_VERSION=4.06.0+fp+flambda
  global:
  - MAKEFLAGS="-j 2"

cache:
  directories:
  - $HOME/.opam